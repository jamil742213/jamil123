#include<stdio.h>
#include<string.h>
int main()
{
    char name[30]="jamil",name1[30]="aktar",s;
    int i = 0, j = 0, len1,len2;
    // printf("Enter string ");
    // gets(name);
    // printf("Enter string2 ");
    // gets(name1);
    // // scanf("%s ",name);
    // // printf("%s",name);
    // printf("%.5s full\n",name);
    // puts(name);
    // puts(name);

    // count = strlen(name);
    // printf("string length is : %d",count);
/*
while (name[count] != '\0')
{
    count++;
}
printf("%d",count);*/
//len1 =strlen(name1);
//len2 =strlen(name);
// strcat(name, name1);
// printf("Concatination of string :%s", name);
/*for (int i = 0; i < len2; i++)
{
    name[len1+i]=name1[i];
}
printf("conctatination is :  %s ",name);*/
/*
while (name[i] != '\0')
{
    i++;
}

while (name[j] !='\0' )
{
    name[i]=name1[j];
    i++;
    j++;
}
printf("strcat : %s",name);*/


return 0;
}